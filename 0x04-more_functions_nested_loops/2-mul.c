#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies functions
 * @a: this is first integer
 * @b: this is second integer
 * Return: returns the result
 */

int mul(int a, int b)
{
	return (a * b);
}
